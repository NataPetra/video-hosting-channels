spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/channels-db
    username: postgres
    password: postgres
  liquibase:
    change-log: classpath:db/changelog/changelog-master.xml
  jpa:
    hibernate:
      ddl-auto: none
